name: snowflake_cohort
version: 1.0.0
config-version: 2

profile: snowflake_cohort

model-paths:
  - models

analysis-paths:
  - analyses

test-paths:
  - tests

seed-paths:
  - seeds

macro-paths:
  - macros

snapshot-paths:
  - snapshots

models:
  snowflake_cohort:
    +materialized: table